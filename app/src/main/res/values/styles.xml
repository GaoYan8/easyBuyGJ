<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!-- ActivityHome的样式 -->
    <style name="DefaultHomeActivityStyle">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/activity_background_color</item>
    </style>

    <!-- Activity的样式 -->
    <style name="DefaultActivityStyle">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <!-- 是否在主要用途：常常用于paddingTop，假设 内部有个属性设置了PaddingTop但是滑动的时候就忽视paddingTop的 则使用该属性如设置android:clipToPadding=false： -->
        <item name="android:clipToPadding">false</item>
        <!-- 是否在状态栏下面 -->
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:background">@color/activity_background_color</item>
    </style>


    <!-- 底部菜单的样式 -->
    <style name="BottomTabStyle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>
    <!-- 底部菜单图片的样式 -->
    <style name="ButtomTabImgBtn">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:layout_centerInParent">true</item>
    </style>

    <!-- 横向细线 -->
    <style name="LineHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/color_bg_gray_b1</item>
    </style>

    <!-- 竖向细线 -->
    <style name="LineVertical">
        <item name="android:layout_width">0.5dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/color_bg_gray_b1</item>
    </style>
</resources>
